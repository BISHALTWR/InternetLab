{
    "version": 2,
    "routes": [
      { "handle": "filesystem" },
      { "src": "/(.*)", "dest": "/" }
    ],
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/static-build"
      }
    ]
  }